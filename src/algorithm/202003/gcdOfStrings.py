

class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        str1_size = len(str1)
        str2_size = len(str2)
        if str1_size == 0 or str2_size == 0:
            return ''
        s, e = 0, 1
        cur = str1[s:e]
        if str1_size < str2_size:

        for i in range(str1_size if str1_size < str2_size else str2_size):

    def iterStr(self, str1: str, size: int, str2: str):
        for
